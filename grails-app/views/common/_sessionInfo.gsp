<ul class="config">
    <li class="scope"><span class="label">scope</span><span class="value">${sessionInfo?.scope ?: "Not Set"}</span></li>
    <li class="app_clientid"><span class="label">app_clientid</span><span class="value">${sessionInfo?.app_clientid ?: "Not Set"}</span></li>
    <li class="app_secret"><span class="label">app_secret</span><span class="value">${sessionInfo?.app_secret ?: "Not Set"}</span></li>
    <li class="app_clientid"><span class="label">user_token</span><span class="value">${sessionInfo?.user_token ?: "Not Set"}</span></li>
    <li class="app_secret"><span class="label">user_refresh</span><span class="value">${sessionInfo?.user_refresh ?: "Not Set"}</span></li>
</ul>
